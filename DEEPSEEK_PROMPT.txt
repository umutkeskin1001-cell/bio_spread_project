## 🧬 DEEPSEEK GENIUS TASK: BioSpread Architectural Audit - PHASE 3

### 🔴 STATUS CHECK: WE HIT A GLASS CEILING
We implemented your Phase 2 plan (Backcast features, FusionNet, Focal Loss). It worked: AUC went from **0.58 to 0.65**. Precision jumped to **0.71**. The model is now "sane" and no longer biased.

**HOWEVER, 0.65 IS STILL NOT GOOD ENOUGH.** 
The target is 0.75+. We are still in the "mediocre" zone. The model converges quickly (Loss 0.16) but hits early stopping because the validation AUC stops improving.

### 🐛 THE RESIDUAL BOTTLENECK
Even with backcast features (epidemiological pressure), the model is missing the **biological "why"**. We have the "how it spread so far" (history) but we are still weak on the "what makes this specific plasmid a monster" (intrinsic fitness).

### 🧠 YOUR MISSION: THE "RADICAL" PHASE 3 PLAN
I need you to think deeper. Much deeper. We need a **Phase 3** plan that doesn't just "add more features" but changes how the model **perceives the biological threat**.

**Consider these radical directions:**
1. **Biological Pre-training (The "Gene-GPT" Approach)**: Should we use a Masked Language Model (MLM) pre-training on ALL 200k+ plasmid gene sequences to learn a "Universal Plasmid Grammar" before the spread task?
2. **Topology-Aware Snapshots**: We currently treat countries as independent. Can we inject a **Geographical Adjacency Matrix** (distance between countries) so the model understands that spreading from France to Germany is different from France to Japan?
3. **Multi-Scale Temporal Windows**: Instead of one snapshot, should we use a **Recurrent (GRU/LSTM) or Temporal Convolutional (TCN)** block over a sequence of snapshots to see the "acceleration" of the spread?
4. **Genetic Interaction (Epistasis) Maps**: The current `GeneEncoder` sees genes as tokens. Can we model **Co-occurrence Networks** where certain combinations of genes (AMR + High-conjugation) trigger a "High-Risk" state?

**Review the `FusionNet` in `evidential.py` and the `REDESIGN_REPORT.md`. Propose a Phase 3 plan that is truly "ingenious" and potentially Nobel-worthy in the context of computational epidemiology.**
